source 'https://rubygems.org'

require 'pp' #https://github.com/defunkt/fakefs/issues/99

gemspec

group :test do
  gem 'rspec', :require => false
  gem 'simplecov', :require => false
  gem 'fakefs', '>= 0.8.1', :require => 'fakefs/safe'
  gem 'webmock'
end